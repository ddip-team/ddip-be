apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: T
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      timeInterval: 10s
      httpMethod: GET